import { makeStyles } from '@material-ui/core/styles';
const useStyles = makeStyles({
    root: {
      width: '100%',
    },
    container: {
      maxHeight: 440,
    },
    carContainer:{
      marginTop:'1rem',
    },
    card:{
      marginTop:`1rem`
    },
    searchBar:{
      minWidth:`100px`
    }
  });

  export default useStyles;