import {
    makeStyles
} from '@material-ui/core/styles';

const useStyles = makeStyles((theme) => ({
    container: {
        backgroundColor: "red",
        marginLeft: "auto",
        marginRight: "auto",
    }
}));

export default useStyles;