import {
    makeStyles
} from "@material-ui/core/styles";

const useStyles = makeStyles((theme) => ({
    formcontainer: {
        width: "40%",
        height: "auto",
        marginLeft: "auto",
        marginRight: "auto",
    },
    status: {
        fontSize: '24px',
        textAlign: 'center',
    },




}));

export default useStyles;